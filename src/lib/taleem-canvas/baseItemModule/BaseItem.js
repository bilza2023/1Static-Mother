

// This is just for exporting the configuration of BaseITem, this is where changes happen
import BaseItemCalculator from "./BaseItemCalculator";

export default class BaseItem extends BaseItemCalculator {

    constructor(itemData) {
        super(itemData)
    }
    
}